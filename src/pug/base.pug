doctype html
html(lang='ja')
  head
    meta(charset='utf-8')/
    title #{htmlTitle}
    link(rel='stylesheet', href='./style.css')/
    script(src='./bundle.js')
  #barba-wrapper
    #cover1
      #cover2
        each i in [...Array(12).keys()]
          img.cover-code(src=`./img/theme/code-${i}.png`, alt='')/
        img#now-loading(src='./img/theme/NowLoading.png', alt='')/
        img#loader(src='./img/theme/loader.png', alt='')/
    #background-code
      each i in [...Array(12).keys()]
        img(src=`./img/theme/code-${i}.png`, alt='')/
    block main
    footer
      a.no-barba(href='http://www.ed.tus.ac.jp/tuscom') TUSCOM
      address
        | 連絡先:
        a.no-barba(href='mailto:tuscom.noda@gmail.com') tuscom.noda@gmail.com
